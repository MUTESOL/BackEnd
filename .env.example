# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet
CLUSTER=devnet

# Smart Contract Program ID (StackSave on Solana Devnet)
PROGRAM_ID=4Ho2aKGXYt4ZyS6qpkxy2xpYpbWJGDQq4grrcnLpJnNR

# Token Mint Addresses (Get these from your deployment)
# Base currency mints (USDC, IDRX)
USDC_MINT=<your-usdc-mint-address>
IDRX_MINT=<your-idrx-mint-address>

# Savings token mints (sUSDC, sIDRX) - created by initialize_savings_mint
SUSDC_MINT=<your-susdc-mint-address>
SIDRX_MINT=<your-sidrx-mint-address>

# Faucet Configuration (for Devnet testing)
FAUCET_ENABLED=true
USDC_FAUCET_AMOUNT=1000
IDRX_FAUCET_AMOUNT=15000000

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/mutesol
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mutesol
DB_USER=postgres
DB_PASSWORD=your_password
DB_SSL=false

# Optional: Wallet for backend operations (if needed for admin functions)
# ADMIN_WALLET_PRIVATE_KEY=<your-private-key-base58>

# Logging
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
